#@tool
extends Node3D

@export var target: Node3D

var distance: float = 2
var theta: float = PI / 4


func _process(delta):
#	var offset = Vector3(2, 0, 0)
#
#	var pivot_point: Vector3 = target.global_position
#	print(global_position)

#	print(get_tree().)

#	global_position = Vector3 (
#		sin(theta) * distance,
#		position.y,
#		cos(theta) * distance,
#	) + Vector3(-1, 0, -2)
##
#	theta += delta



#	transform = pi

#	angle+= delta

#	theta = atan()



#	global_transform = target.global_transform * Transform3D().rotated(Vector3.ZERO.normalized(), angle)



	pass
